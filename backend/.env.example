# Backend Environment Variables
# Copy this file to .env for local development

# Database Connection
# For local: postgresql://postgres:postgres@localhost:5432/domunity
# For Render: Automatically set from connected database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/domunity

# Server Configuration
# Render sets PORT automatically (default: 8080)
# For local development, you can use 50051
PORT=50051

# Logging Level
# Options: error, warn, info, debug, trace
# Format: level,module=level (e.g., info,domunity_backend=debug)
RUST_LOG=info,domunity_backend=debug

# JWT Secret for Token Signing
# Generate with: openssl rand -hex 32
# MUST be at least 32 characters
# NEVER commit real secret to git!
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-minimum-32-chars

# Frontend URL for CORS
# For local: http://localhost:5173
# For Render: https://domunity-frontend.onrender.com
FRONTEND_URL=http://localhost:5173

# Optional: Token Expiration (in seconds)
# Default: 3600 (1 hour)
# JWT_EXPIRATION=3600
